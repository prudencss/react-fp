export enum EButtonType {
  Basic = 'c-btn-basic',
  NoBorder = 'c-btn--no-border',
  Stroked = 'c-btn--stroked',
  Flat = 'c-btn--flat',
  Ghost = 'c-btn--ghost',
};

export enum EIntrinsicButtonType {
  Button = 'button',
  Submit = 'submit',
  Reset = 'reset',
};
